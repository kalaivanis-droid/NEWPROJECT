#include <stdio.h>

int main() {
    int a, b, c, sum;

    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b); // fetch input

    sum = a + b;
    c=sum+a;
    printf("Sum = %d\n", sum);
    printf("C=%d",c);a

    return 0;
}
