#include <stdio.h>

int main() {
    int a, b, c, sum;

    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b); // fetch input

    sum = a + b;
    c=sum+a;
    printf("Sum = %d\n", sum);
    printf("C=%d",c);// display output
printf("WECOME");
    return 0;
    sum=a-b;
    printf("Difference=%d\n",sum);
}
